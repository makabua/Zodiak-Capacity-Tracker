# Port the Express server listens on (Railway sets this automatically).
PORT=3000

# Path to the SQLite database file.
# In production, mount a persistent volume (e.g. Railway volume at /data) and
# set DB_PATH=/data/carriers.db so your data survives redeploys.
DB_PATH=./data/carriers.db
